<%- include('../partials/header')%>

<body>
    <nav class="navbar navbar-dark bg-primary">
        <a href="/" class="navbar-brand">
            Snap
        </a>
        
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a  class="nav-link" href="/logout" style="margin-right: 15px;">Logout</a>
            </li>
        </ul>
    </nav>

    <div class="container">
        
        <br>

        <div class="mt-4 p-5 bg-primary text-white rounded">
            <h1 class="display-4">Bem-vindo a página de personalização de sua conta!</h1>
            <p class="lead">Aqui, você pode editar informações como email, nome de usuário e senha</p>
            <hr class="my-4">
            <p>
                Se a alteração for bem-sucedida, você será redirecionado para a página inicial.
                Caso contrário, irá retornar para essa mesma tela. 
            </p>
            <p>
                <button data-bs-toggle="collapse" data-bs-target="#userc" class="btn btn-secondary">Editar nome de usuário</button>
                <button data-bs-toggle="collapse" data-bs-target="#usere" class="btn btn-secondary">Editar email</button>
                <button data-bs-toggle="collapse" data-bs-target="#userp" class="btn btn-secondary">Trocar senha</button>
            </p>
          </div>

        <br>
        
        
        <div class="collapse" id="userc">
            <div class="card" id="usernameChange">
                <div class="card-header">
                    <h2>Editar nome de Usuário</h2>
                </div>
                <div class="card-body">
                    <form action="/user/name" method="POST">
                        <input type="hidden" name="id" value="<%= user.id %>">
                        <input type="text" class="form-control" name="name" placeholder="Insira seu nome" value="<%= user.name %>">
                        <br>

                        <button class="btn btn-primary">Confirmar</button>
                    </form>
                </div>
            </div>
        </div>

        
        <div class="collapse" id="usere">
            <div class="card" id="emailChange">
                <div class="card-header">
                    <h2>Modificar email</h2>
                </div>
                <div class="card-body">
                    <form action="/user/email" method="POST">
                        <input type="hidden" name="id" value="<%= user.id %>">
                        <input type="text" class="form-control" name="email" placeholder="Insira seu email" value="<%= user.email %>">
                        <br>
                        <input type="password" class="form-control" name="confPass" placeholder="Confirme sua senha (caso a operação falhe, voltará a essa tela)">
                        <br>
                        <button class="btn btn-primary">Confirmar</button>
                    </form>
                    
                </div>
            </div>
        </div>
        
        <div class="collapse" id="userp">
            <div class="card" id="passChange">
                <div class="card-header">
                    <h2>Trocar senha de usuário</h2>
                </div>
                <div class="card-body">
                    <form action="/user/password" method="POST">
                        <input type="hidden" name="id" value="<%= user.id %>">
                        <input type="password" class="form-control" name="confPass" placeholder="Digite sua senha antiga">
                        <br>
                        <input type="password" class="form-control" name="newPass" placeholder="Digite sua nova senha (caso a operação falhe, voltará a essa tela)">
                        <br>
                        <input type="password" class="form-control" name="passControl" placeholder="Confirme sua nova senha">
                        <br>
                        <button class="btn btn-primary">Confirmar</button>
                    </form>
                </div>
            </div>
        </div>
        

    </div>
</body>
<%- include('../partials/footer')%>